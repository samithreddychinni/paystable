name: Release

on:
  push:
    tags: ['v*']

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: '1.23'
          cache: false

      - name: Build dashboard
        working-directory: dashboard
        run: |
          npm ci
          npm run lint
          npm run build
          rm -rf ../internal/ui/dist
          cp -r dist ../internal/ui/dist

      - name: Cross-compile
        run: |
          mkdir -p dist
          for GOOS in linux darwin; do
            for GOARCH in amd64 arm64; do
              OUT="dist/paystable-${GOOS}-${GOARCH}"
              GOOS=$GOOS GOARCH=$GOARCH CGO_ENABLED=0 go build -ldflags="-s -w -X main.Version=${{ github.ref_name }}" -o "$OUT" ./cmd/paystable
              echo "Built $OUT"
            done
          done
          (cd dist && sha256sum paystable-* > checksums.txt)

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: dist/*
          generate_release_notes: true
